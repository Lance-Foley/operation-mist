<%= form_with(model: crew) do |form| %>
  <% if crew.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(crew.errors.count, "error") %> prohibited this crew from being saved:</h2>

      <ul>
        <% crew.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="row">

    <div class="col">
      <%= form.label :name, style: "display: block", class: "form-label" %>
      <%= form.text_field :name, class: "form-control" %>
    </div>
    <div class="col">
      <%= form.label :division_id, style: "display: block", class: "form-label " %>
      <%= form.collection_select(:division_id, Division.all.collect, :id, :name, {}, class: "form-select") %>

      <%#= form.select :crew_id, Crew.all.collect, :id, class: "form-select" %>
    </div>


    <div>
    <span>
      <hr>
      <%= form.submit "Submit", class: "btn btn-warning float-end" %>
    </span>
    </div>
<% end %>
</div>
