<%= form_with(model: variance) do |form| %>
  <% if variance.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(variance.errors.count, "error") %> prohibited this variance from being saved:</h2>

      <ul>
        <% variance.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="container">

    <div class="form-group">
      <label class="form-label mt-4">Variance</label>
      <div class="form-group">

        <div class="input-group mb-3">
          <%= form.text_field :variance, class: "form-control", placeholder: @variance.variance %>
          <%= form.submit "Update", class: "btn btn-primary" %>

        </div>
      </div>
    </div>

  </div>
 

<% end %>
